# Production environment for docker-compose.prod.yml
# Copy to .env.prod and adjust before deploying:
#   cp .env.prod.example .env.prod
#
# Generate a fresh APP_KEY with:
#   docker run --rm php:8.3-cli php -r "echo 'base64:'.base64_encode(random_bytes(32)).PHP_EOL;"

APP_KEY=base64:AqOEj3ib5/1PvnxA5EUClR+bbFgHdrZp9qh3sGQayMY=
APP_URL=http://localhost

DB_DATABASE=shopping_simulator
DB_USERNAME=shopping
DB_PASSWORD=change_this_password

# Only used when running the optional "monitoring" profile.
GRAFANA_USER=admin
GRAFANA_PASSWORD=change_this_password
